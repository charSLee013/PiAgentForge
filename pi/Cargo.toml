[package]
name = "pi"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
feat-all = ["feat-openai", "feat-anthropic", "feat-google", "feat-mistral", "feat-bedrock"]
feat-extensions = ["dep:pi-extension-system", "pi-tui-app/feat-extensions"]
feat-openai = []
feat-anthropic = ["pi-cli/feat-anthropic"]
feat-google = []
feat-mistral = []
feat-bedrock = ["pi-cli/feat-bedrock"]

[dependencies]
pi-cli = { path = "../pi-cli" }
pi-core = { path = "../pi-core" }
pi-oauth = { path = "../pi-oauth" }
pi-agent-core = { path = "../pi-agent-core" }
pi-ai-core = { path = "../pi-ai-core" }
pi-model-catalog = { path = "../pi-model-catalog" }
pi-modes = { path = "../pi-modes" }
pi-provider-openai = { path = "../pi-provider-openai" }
pi-tui-app = { path = "../pi-tui-app" }
tokio.workspace = true
anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
clap.workspace = true
dirs = "5"
chrono.workspace = true
async-trait.workspace = true
pi-extension-system = { path = "../pi-extension-system", optional = true }
